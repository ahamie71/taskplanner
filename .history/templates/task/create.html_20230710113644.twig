{% extends 'base.html.twig'%}

{% block body %}

   <h1> Create a task </h1>

    {{form_start(formTask) }}

   {{form_row(formTask.title, {'attr':{'placeholder':"Title of the task"}})}}

    {{form_row(formTask.category.nom, {'attr':{'placeholder':"Category of the task"}})}}

     {{form_row(formTask.description, {'attr':{'placeholder':"Description of the task"}})}}

   

   

<button type="submit" class="btn btn-success">
     {% if editMode %}
        
        Enregistrer les modification
         
         {% else %}

     Ajouter l'article
     {% endif %}
     </button>


{{form_end(formArticle)}}

</div>




{% endblock %}